node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*.log
logs/
launch-artifacts/
server.mvp.*.log
.config/
.local/
.env
.env.local
.env.*
!.env.example
*.env
attached_assets/**/*.env
attached_assets/documents/*mvp-*.txt
attached_assets/documents/*mvp-*.txt.metadata.json
__pycache__/
*.py[cod]
*api-key*.txt
*apikey*.txt

# Agent skills installed via `npx skills add`. Third-party content that runs
# with full agent permissions; kept out of this public repo. Scoped to
# .claude/skills/ rather than all of .claude/ so project settings can still be
# tracked there.
.agents/
.claude/skills/
skills-lock.json
