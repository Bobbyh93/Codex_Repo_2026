# Superseded by v3

This folder is **pass 2**: the first 10 rows regenerated semantically, as a proof that candidate generation — not the drafting model — was the bottleneck.

**Do not review from this folder.** The current table is:

    ..\mapping_resolution_claude_v3\wps_qsen_crosswalk_v3_review.csv

v3 carries these 10 rows forward unchanged and adds the other 26 on the same method, so the whole 36-row table now uses one candidate-generation approach. Its builder reads `candidate_regeneration_v2.json` from this folder rather than duplicating it — **do not delete that file.**

Kept as history and for `README_V2.md`, which records the before/after on these 10 rows.
