# Upgrade notes v1.5

- Added a fixed intake folder for Data Chunker Pro output.
- Added a bundled taxonomy folder and default master taxonomy base so a separate taxonomy upload is not required.
- Replaced placeholder concept/category map files with generated concept-aware baseline files derived from the bundled topic catalog.
- Added app endpoints and user-interface controls for processing the default intake folder and using the bundled taxonomy source.
