Recommended setup

Set Data Chunker Pro output folder to:
  intake\data_chunker_output

Then in the app use:
  Step 1 -> Data Chunker output folder -> Process app intake folder

The app will scan JSON chunk files in that folder recursively, detect concepts, and build a source table automatically.
